<h2>Beers page</h2>
<ul>
<% for(let index=0; index < array.length;index++) {%>
    <li>
        <img src="<%= beer.image_url %>" alt="<%= beer.name %>">
        <h2><%= beer.name %></h2>
        <p><%= beer.description %></p>
        <p><%= beer.tagline %></p>
      </li>  <%} %>
</ul>